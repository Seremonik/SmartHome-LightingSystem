#ifndef CONFIG_H
#define CONFIG_H

    const int ShutterAutoHoldTimeMiliseconds = 20000; // for how long system should hold the button?
    const int ShutterButtonHoldTimeMiliseconds = 1500; // how long it takes user to hold a button to initiate a auto hold

#endif